part of '../../sign_up_presentation.dart';

class _NameScaffold extends StatelessWidget {
  const _NameScaffold();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('data'),
    );
  }
}
